# Eagle Eyes SLV — Open Threads & Next Steps
*Written August 8, 2026, ~1am, at the end of the CDOT integration session.*

## Shipped tonight

- **Two silent space-weather bugs fixed** — desktop Kp parsed NOAA's feed as an
  array-of-arrays (it's array-of-objects) and rendered `NaN`; the solar-wind
  endpoint had been **dead on both pages** (NOAA retired the entire
  `/products/solar-wind/` directory) and failed invisibly behind a bare
  `if (r2.ok)`. Both now on `/products/summary/solar-wind-speed.json` with loud
  failure reporting.
- **CDOT live road incidents** on desktop + mobile, via a Cloudflare Worker proxy
  (`worker/`), 2-minute refresh, closures raise alerts.
- **Basemap switcher** — Satellite / Topographic / Streets + a composable road-name
  labels overlay, persisted in `localStorage`, on both pages.

---

## Loose ends — real, named, not smoothed over

### 1. Worker bbox filter is committed but NOT deployed
`b748bbf` adds server-side bounding-box filtering to the Worker. It is **not live** —
the Cloudflare quick-edit editor refused three scripted paste attempts (`Ctrl+A`
kept selecting the page rather than the Monaco iframe, which is cross-origin and
can't be scripted into).

**Why it matters:** `roadConditions` returns **~3.6 MB** statewide (dense LineString
geometry). That's unacceptable over cell data, which is why the roadConditions
layer is *not yet added*. `incidents` is only ~10 KB, so tonight's feature is
unaffected.

**To deploy:** open the Worker's *Edit code*, select all, paste the current
`worker/cotrip-proxy.js`, Deploy. A manual `Ctrl+V` is a real user gesture and
will likely work where automation didn't.

### 2. `fetchMinerals` times out on the desktop page
Throws `TypeError: Failed to fetch`. **Pre-existing, not from tonight.** Checked:
the USGS endpoint is up (HTTP 200) and sends `Access-Control-Allow-Origin: *`, but
takes **~12 seconds**. So it's a timeout, not an outage. Fix is probably an
explicit `AbortController` timeout + a smaller WFS query, not a new data source.

### 3. Three camera locations still have no real CDOT coverage
Villa Grove, Cumbres Pass, North Pass/Cochetopa — kept visible and honestly
labeled rather than silently removed. Poncha Pass is a real camera that was
offline; its image path was deliberately **left empty rather than guessed**. If
CDOT brings it back, re-check `cotrip.org/list/cameras` and fill in the real path.

### 4. Camera marker coordinates are inferred, not sourced — several are wrong
**Reported by James, Aug 8 2026, from the ground:** the Antonito CO 17 camera pins to a
dirt road, not the camera's actual location. Assume others are off too.

**Root cause, owned plainly:** every `lat`/`lon` in `CDOT_CAMS` was hand-placed during
Session 124 by reasoning from route name and milepost. None came from CDOT's own
geometry. They were *plausible* rather than *verified* — the same inferred-vs-sourced
error as the guessed camera image URL caught that same night, except this one shipped
because nothing checked it. A marker that is confidently in the wrong place is worse
than no marker: it tells you a pass is clear when you're looking at somewhere else.

**The fix, and it should be a real one — not more eyeballing:** cotrip.org's own
GraphQL endpoint returns Camera features *with geometry*, which is authoritative
CDOT data. The query is already captured in this session's work:

```
POST https://www.cotrip.org/api/graphql
query MapFeatures($input: MapFeaturesArgs!) { mapFeaturesQuery(input: $input) {
  mapFeatures { bbox title tooltip uri features { id geometry properties type }
  ... on Camera { active views(limit: 5) { uri ... on CameraView { url } category } } }
  error { message type } } }
variables: { input: { north, south, east, west, zoom, layerSlugs: ["cameras"],
  nonClusterableUris: [] } }
```

Run it **once, offline** (curl/Node — it needs no key; it's only unusable from the
browser because it sends no CORS headers) over the SLV bounding box, then replace the
hardcoded coordinates with CDOT's real ones. This is a one-time data correction, not a
runtime dependency — no new fetch on page load.

**While doing it, also recheck:** whether Poncha Pass is back online (its image path was
deliberately left empty rather than guessed), and whether the three "no coverage"
locations (Villa Grove, Cumbres Pass, North Pass/Cochetopa) have since gained cameras.
The same query answers all of it, and the `views` field gives real image URLs — so the
whole camera list can be regenerated from source rather than maintained by hand.

**Standing lesson:** map coordinates are data and need a source like any other claim.
"Near milepost 39 on CO 17" is a description, not a location.

### 5. Cloudflare dashboard tab may hold unsaved editor changes
From the failed paste attempts. **Don't hit Deploy in it** — close and discard.

---

## Next steps, roughly in value order

### A. Regenerate the camera list from CDOT geometry (see loose end 4)
Small, high-confidence, and fixes a correctness bug James found on the ground. One
offline GraphQL call replaces 12 hand-guessed coordinates with CDOT's own, and
simultaneously refreshes which cameras exist and their real image paths. Do this before
adding more camera features -- everything built on top of wrong coordinates inherits them.

### B. Road surface conditions from RWIS (`weatherStations`) — highest value
Already in the subscription, already proxied, **not yet used**. CDOT's roadside
weather stations report **road surface temperature and condition** — which is the
actual question in winter: not "is it snowing" but "is US 160 over La Veta iced".
This is the single most valuable unused feed for someone who actually drives these
passes.

### C. `roadConditions` layer — gated on deploying the bbox filter (loose end 1)
Statewide winter driving conditions per segment. Deploy the filter first, then add
as a line overlay, mobile included.

### D. `plannedEvents` — construction and closures
Lower urgency than incidents but genuinely useful for trip planning, especially the
CO 17 paving work.

### E. Correlate cameras with incidents
When an incident is near a camera, offer that camera's live image in the incident
inspector. Both datasets are already loaded — this is pure UI, no new fetching, and
turns "there's a crash on US 160" into "here's what it looks like."

### F. Alert quality pass
Right now any closure raises an alert. Worth weighting by proximity to Antonito and
by whether it's on a route you actually use (US 285, US 160, CO 17) rather than
treating a Front Range closure the same as a local one.

---

## Standing lessons this app keeps teaching

- **A 200 is not a success.** Three separate times tonight a success status meant
  something other than what it looked like: a redirect stub, a GraphQL query that
  returns a clean `0` for a *bogus* layer name, and an Angular SPA shell served for
  every path including `/api/v1/incidents`. Check the body, not the status.
- **Absence of data must announce itself.** The dead NOAA endpoint hid behind
  `if (res.ok)` with no `else`. Every guarded network call needs a failure branch
  that at minimum warns, plus a payload-shape guard.
- **Test the path that doesn't run.** With zero local incidents, the marker-drawing
  code would never have executed and would have shipped broken. Exercising it
  against real statewide data found a genuine bug: CDOT returns **Point AND
  MultiPoint**, and the first filter silently dropped every MultiPoint — i.e.
  exactly the segment-spanning closures that matter most.
- **"Saved" is not "deployed."** In Cloudflare, saving a secret creates a *version*;
  the running deployment keeps its old bindings until the new version is promoted.
